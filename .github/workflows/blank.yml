name: Greeting on variable day

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  DAY_OF_WEEK: Hello i am a param

jobs:
  greeting_job:
    runs-on: windows-2019

    steps:
      - name: "Say Hello Mona it's Monday"
        run: write-host $Env:DAY_OF_WEEK