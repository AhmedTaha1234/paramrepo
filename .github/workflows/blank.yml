name: Octo Organization CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  key: i am a param

      - name: Run a one-line script
        run: | 
           . ./.github\workflows\sayparams.ps1
           deploydb ${{ key }} 